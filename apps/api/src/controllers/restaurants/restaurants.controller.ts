export const restaurantsController = {
  // list: () => restaurantsService.list(),
  // getById: (id: string) => restaurantsService.getById(id),
  // create: (body: unknown) => restaurantsService.create(body),
};
