{
  "name": "@wigarn/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --format cjs",
    "check-types": "tsc --noEmit",
    "migrate:dev": "drizzle-kit migrate",
    "generate": "drizzle-kit generate",
    "db:seed": "bun src/seeds.ts"
  },
  "devDependencies": {
    "@faker-js/faker": "^10.4.0",
    "drizzle-kit": "^0.31.10"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "@neondatabase/serverless": "^1.1.0",
    "pg": "^8.0.0"
  }
}
